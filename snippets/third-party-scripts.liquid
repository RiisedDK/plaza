{%- if settings.cookie_information_enabled -%}
  {% render 'cookie-information-consent' %}
{%- endif -%}

{%- if settings.gtm_container_id != blank and settings.gtm_url != blank -%}
  <!-- Google Tag Manager -->
  <script type="lazyload_js">
    (function (w, d, s, l, i) {
      w[l] = w[l] || [];
      w[l].push({ 'gtm.start': new Date().getTime(), event: 'gtm.js' });
      var f = d.getElementsByTagName(s)[0],
        j = d.createElement(s),
        dl = l != 'dataLayer' ? '&l=' + l : '';
      j.async = true;
      j.src = '{{ settings.gtm_url }}/gtm.js?id=' + i + dl;
      f.parentNode.insertBefore(j, f);
    })(window, document, 'script', 'dataLayer', '{{ settings.gtm_container_id }}');
  </script>
  <!-- End Google Tag Manager -->
{%- endif -%}
